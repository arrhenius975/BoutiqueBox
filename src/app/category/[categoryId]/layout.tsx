
// This layout for dynamically fetched category product pages
// is being temporarily removed alongside its corresponding page.tsx.
export default function DeprecatedCategoryProductsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
